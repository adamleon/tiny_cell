# Build
build/
build-msvc/
build-mingw/

# CMake user-specific files
CMakeUserPresets.json

# Editor — track shared config (launch / tasks / extensions); ignore per-user files
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Tool caches
.cache/
compile_commands.json

# Future: package manager
vcpkg_installed/

# Claude Code — track shared project settings; ignore per-user overrides + caches
.claude/*
!.claude/settings.json
