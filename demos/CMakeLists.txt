# Demos are per-milestone, each its own executable.
# Add a new subdirectory here when starting a new milestone demo.
add_subdirectory(hello)
add_subdirectory(arm_catalog)
add_subdirectory(arm_strategy)
add_subdirectory(brute_force_enumerator)
add_subdirectory(state_propagation)
add_subdirectory(layer_2_allocation)
add_subdirectory(transport_workflow)
